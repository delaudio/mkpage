# Theme starter pack

This folder contains a terminal-inspired, static CSS reference you can use with mkpage
layouts and widgets.

- Copy `site.css` to `static/css/site.css` in your mkpage project.
- Ensure your layout links to `/css/site.css`.
- Keep widget class names from `docs/widgets.md`; this stylesheet applies those
  contracts.
