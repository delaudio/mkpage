# Contributing

Read `AGENTS.md` and the product contract before starting substantive work.

Use Conventional Commits. For changes to Rust code, run formatting, Clippy,
tests, and a release build before pushing. Keep the public library boundary
independent of CLI-only dependencies.
